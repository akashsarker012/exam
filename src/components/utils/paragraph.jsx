import React from 'react'

export default function Paragraph({ text, className, }) {
  return (
    
      <paragraph className={`text-[#645E76] text-lg font-normal ${className}`}> {text}</paragraph>
    
  )
}
